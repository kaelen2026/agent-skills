# 功能规格书模板

> 使用方法：复制此模板，替换 `{占位符}` 为实际内容。删除不适用的章节。

---

# {功能名称} — 功能规格书

## 1. 概要

**一句话描述**: {用一句话说明这个功能做什么}

**背景**: {为什么需要这个功能？解决什么问题？}

**目标用户**: {谁会使用这个功能}

## 2. 用户故事

```
作为 {角色}，
我希望 {操作}，
以便 {收益}。
```

| ID | 角色 | 操作 | 收益 | 优先级 |
|----|------|------|------|--------|
| US-01 | {角色} | {操作} | {收益} | MUST |
| US-02 | {角色} | {操作} | {收益} | SHOULD |
| US-03 | {角色} | {操作} | {收益} | COULD |

## 3. 功能需求

### 3.1 MUST（必须）

- [ ] {必须实现的核心功能 1}
- [ ] {必须实现的核心功能 2}

### 3.2 SHOULD（应该）

- [ ] {应该实现但可推迟的功能 1}
- [ ] {应该实现但可推迟的功能 2}

### 3.3 COULD（可选）

- [ ] {锦上添花的功能 1}

## 4. API 变更

> 如无 API 变更，删除此章节。

### 4.1 端点总览

| Method | Path | Description | Auth |
|--------|------|-------------|------|
| {METHOD} | {/v1/path} | {描述} | {Yes/No} |

### 4.2 请求/响应详情

#### {METHOD} {/v1/path}

**请求:**

```json
{
  "field": "type — 说明"
}
```

**成功响应 (200):**

```json
{
  "success": true,
  "data": {},
  "meta": {
    "requestId": "uuid"
  }
}
```

**错误响应:**

| 状态码 | 错误码 | 说明 |
|--------|--------|------|
| 400 | VALIDATION_ERROR | {描述} |
| 401 | UNAUTHORIZED | {描述} |
| 404 | NOT_FOUND | {描述} |
| 409 | CONFLICT | {描述} |

## 5. DB 变更

> 如无 DB 变更，删除此章节。

### 5.1 表结构

```sql
CREATE TABLE {table_name} (
  id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  {column}    {TYPE} {CONSTRAINTS},
  created_at  TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

### 5.2 索引

```sql
CREATE INDEX idx_{table}_{column} ON {table_name} ({column});
```

### 5.3 迁移策略

- 迁移方式: {零停机 / 维护窗口}
- 回滚计划: {如何回滚}
- 数据迁移: {是否需要数据迁移}

## 6. 非功能需求

| 类别 | 目标 | 度量方式 |
|------|------|----------|
| 性能 | {如: API 响应 < 200ms P95} | {监控工具/方法} |
| 安全 | {如: 所有端点需认证} | {审查方式} |
| 可访问性 | {如: WCAG 2.1 AA} | {测试工具} |
| 可用性 | {如: 99.9% uptime} | {监控告警} |
| 扩展性 | {如: 支持 10K 并发用户} | {负载测试} |

### 6.1 性能详细指标

| 端点/操作 | Latency 预算 (P95) | Latency 预算 (P99) | 吞吐量目标 |
|-----------|---------------------|---------------------|------------|
| {GET /v1/resource} | {< 200ms} | {< 500ms} | {1000 req/s} |
| {POST /v1/resource} | {< 300ms} | {< 800ms} | {500 req/s} |
| {批量操作} | {< 2s} | {< 5s} | {50 req/s} |

**数据库查询时间限制**:

| 查询类型 | 时间限制 | 超限处理 |
|----------|----------|----------|
| 单行查询 (by PK/index) | {< 10ms} | {告警 + 日志} |
| 列表查询 (分页) | {< 50ms} | {告警 + 强制分页} |
| 聚合/报表查询 | {< 500ms} | {异步化 / 缓存} |
| 全文搜索 | {< 100ms} | {降级到前缀匹配} |

### 6.2 安全详细要求

**认证要求**:

| 端点 | 认证方式 | 授权级别 | 备注 |
|------|----------|----------|------|
| {/v1/public/*} | {无需认证} | — | {公开端点} |
| {/v1/resource} | {Bearer Token (JWT)} | {user 级别} | {仅可访问自己的数据} |
| {/v1/admin/*} | {Bearer Token + RBAC} | {admin 角色} | {需要管理员权限} |

**数据加密**:

| 数据类型 | 静态加密 (at rest) | 传输加密 (in transit) | 密钥管理 |
|----------|-------------------|----------------------|----------|
| {用户 PII} | {AES-256, 列级加密} | {TLS 1.3} | {KMS 托管, 90 天轮换} |
| {会话 Token} | {不存储明文} | {TLS 1.3} | {签名密钥每 24h 轮换} |
| {日志数据} | {磁盘加密} | {TLS 1.3} | {基础设施级别} |

**审计日志**:

| 事件类型 | 记录内容 | 保留期限 |
|----------|----------|----------|
| {认证事件} | {userId, IP, timestamp, success/failure} | {90 天} |
| {数据变更} | {userId, resource, action, before/after} | {365 天} |
| {管理操作} | {adminId, action, target, reason} | {永久} |

### 6.3 可用性要求

| 指标 | 目标 | 度量周期 |
|------|------|----------|
| SLA 可用性 | {99.9% (每月停机 < 43 分钟)} | {月} |
| 计划内维护窗口 | {每月最多 {N} 次, 每次 < {M} 分钟} | {月} |

**故障转移策略**:

| 故障场景 | 检测方式 | 转移策略 | 预计恢复时间 |
|----------|----------|----------|-------------|
| {单实例宕机} | {健康检查 / 心跳} | {自动切换到备用实例} | {< 30s} |
| {数据库主节点故障} | {复制延迟监控} | {自动提升只读副本} | {< 60s} |
| {整区域故障} | {外部探针} | {DNS 切换到灾备区域} | {< 15 分钟} |

**灾难恢复**:

- RTO (恢复时间目标): {4 小时}
- RPO (恢复点目标): {< 1 小时, 即最多丢失 1 小时数据}
- 备份策略: {每日全量 + 每小时增量, 跨区域存储}
- 恢复演练: {每季度一次, 记录实际 RTO/RPO}

### 6.4 国际化要求

| 需求项 | 规格 | 备注 |
|--------|------|------|
| 支持 Locale | {en-US, zh-CN, ja-JP, ...} | {初始支持 / 后续追加} |
| 默认 Locale | {en-US} | {未匹配时的回退} |
| 日期格式 | {遵循 locale 规范, 如 en: MM/DD/YYYY, zh: YYYY-MM-DD} | {使用 Intl.DateTimeFormat} |
| 货币格式 | {遵循 locale 规范, 支持 {USD, CNY, JPY, ...}} | {使用 Intl.NumberFormat} |
| 时区处理 | {服务端 UTC, 客户端本地化} | {存储 TIMESTAMPTZ} |
| RTL 支持 | {是 / 否 / 后续追加} | {如需要: CSS logical properties} |
| 文本方向 | {LTR 为主, RTL 适配 {ar, he}} | {dir="auto" + 布局翻转} |

## 7. 验收标准

每条验收标准必须可测试，格式：

```
GIVEN {前置条件}
WHEN  {操作}
THEN  {预期结果}
```

| ID | 验收标准 | 对应用户故事 |
|----|----------|-------------|
| AC-01 | GIVEN ... WHEN ... THEN ... | US-01 |
| AC-02 | GIVEN ... WHEN ... THEN ... | US-01 |
| AC-03 | GIVEN ... WHEN ... THEN ... | US-02 |

## 8. 范围外事项

以下内容**不在**本次迭代范围内：

- {明确排除的功能 1}
- {明确排除的功能 2}

## 9. 开放问题

| ID | 问题 | 状态 | 决策 |
|----|------|------|------|
| Q-01 | {待决定的问题} | OPEN | — |

## 10. 依赖矩阵

### 10.1 内部依赖

| 依赖项 | 类型 | 所属模块 | 状态 | 影响说明 |
|--------|------|----------|------|----------|
| {共享认证模块} | 共享库 | {auth/} | {已就绪 / 需修改} | {需要扩展 token scope} |
| {用户管理功能} | 功能依赖 | {user/} | {已上线 / 开发中} | {需要 user profile API} |
| {通知服务} | 服务依赖 | {notification/} | {已就绪} | {发送邮件/推送通知} |

### 10.2 外部依赖

| 依赖项 | 版本约束 | 用途 | 替代方案 | 风险等级 |
|--------|----------|------|----------|----------|
| {Stripe API} | {v2024-01+} | {支付处理} | {PayPal / 自建} | {LOW} |
| {SendGrid} | {v3} | {邮件发送} | {AWS SES / Resend} | {LOW} |
| {第三方 OAuth} | {OAuth 2.0} | {社交登录} | {自建认证} | {MEDIUM} |

### 10.3 部署依赖

| 依赖项 | 类型 | 就绪状态 | 负责人 | 完成期限 |
|--------|------|----------|--------|----------|
| {环境变量: {VAR_NAME}} | 配置 | {待配置} | {DevOps} | {上线前 3 天} |
| {数据库迁移} | 基础设施 | {待执行} | {DBA} | {上线前 1 天} |
| {CDN 配置} | 基础设施 | {已就绪} | {DevOps} | — |
| {DNS 记录} | 基础设施 | {待配置} | {DevOps} | {上线前 1 天} |

## 11. 技术债务评估

### 11.1 影响本功能的既有技术债务

| 债务描述 | 影响范围 | 严重程度 | 是否阻塞 |
|----------|----------|----------|----------|
| {现有 API 未使用统一响应信封} | {需要在本功能中兼容两种格式} | HIGH | {否, 但增加工作量} |
| {数据库缺少 {table} 的合适索引} | {查询性能受影响} | MEDIUM | {否, 可同步修复} |
| {测试覆盖率不足的模块: {module}} | {修改时回归风险高} | MEDIUM | {否, 需补充测试} |

### 11.2 本功能可能引入的新技术债务

| 债务描述 | 产生原因 | 严重程度 | 可接受条件 |
|----------|----------|----------|------------|
| {硬编码的业务规则} | {需求不确定, 先快速实现} | LOW | {需求稳定后重构为配置化} |
| {临时的数据兼容层} | {旧数据格式迁移未完成} | MEDIUM | {迁移完成后移除, 期限: {日期}} |
| {未优化的 N+1 查询} | {初版优先功能正确性} | MEDIUM | {上线后 2 周内优化} |

### 11.3 债务偿还计划

| 优先级 | 债务项 | 处理时机 | 预计工作量 | 跟踪方式 |
|--------|--------|----------|------------|----------|
| 立即 | {本次必须修复的债务} | 本次迭代内 | {N 人天} | {Issue #{号}} |
| 下次迭代 | {可推迟但应尽快修复的债务} | {下次迭代日期} | {N 人天} | {Issue #{号}} |
| 长期 | {需要较大重构才能解决的债务} | {Q{N} 规划} | {N 人周} | {Epic #{号}} |

---

*创建日期: {YYYY-MM-DD}*
*最后更新: {YYYY-MM-DD}*
