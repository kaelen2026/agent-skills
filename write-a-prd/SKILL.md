---
name: write-a-prd
description: "交互式 PRD 编写技能。通过用户访谈、代码库探索和深模块设计，生成完整的产品需求文档。"
metadata:
  filePattern:
    - "**/docs/prd/**/*"
    - "**/prd.md"
  priority: 9
---

# Write a PRD

就像记者采访一样——先深挖问题全貌，再写报道。PRD 不是填模板，是通过反复追问把模糊想法变成精确需求。

## When to Activate

- 用户想要定义新功能或产品需求
- 用户说"写 PRD"、"需求文档"、"feature request"
- 需要将模糊想法转化为可执行的需求文档

## Workflow

### 1. 收集问题描述

向用户询问：

- **问题是什么？** 用户遇到的痛点或需要的功能
- **初步方案？** 用户心中的解决思路（如有）
- **目标用户？** 谁会受益

不要急于深入——先拿到全貌。

### 2. 探索代码库

用 Agent (Explore) 或 Grep/Glob 验证用户的断言：

- 现有相关功能的实现方式
- 受影响的模块和边界
- 当前架构约束和技术债

目的：用代码事实校准用户的描述，发现用户未提及的影响面。

### 3. Grill-Me 追问

调用 [grill-me](../grill-me/SKILL.md) 技能，对步骤 1-2 收集到的信息进行深度追问。
追问完成后，将决策摘要作为 PRD 生成的输入。

### 4. 深模块设计

识别主要模块，追求深模块设计（小接口隐藏大量复杂性）：

- 列出 3-5 个核心模块
- 每个模块：接口（输入/输出）+ 隐藏的复杂性
- 模块间的依赖关系
- Schema 变更、API 契约、架构决策

向用户展示模块设计，确认后继续。

**禁止写入 PRD 的内容**：
- 具体文件路径（会过时）
- 代码片段（会过时）
- 实现细节（属于 spec，不属于 PRD）

### 5. 生成 PRD

使用 [references/prd-template.md](references/prd-template.md) 模板生成 PRD。

**输出方式**：直接输出 PRD 文档内容，保存到 `docs/prds/` 目录。

**测试决策原则**：只描述外部行为的验证，不描述实现细节的测试。

## Design Review

生成后自动检查：

- [ ] 问题描述清晰，非技术人员也能理解
- [ ] 用户故事覆盖所有角色和主要场景
- [ ] MoSCoW 优先级已标记
- [ ] 模块设计只含接口和架构决策，无文件路径/代码
- [ ] Schema 变更和 API 契约已定义
- [ ] 验收标准测试外部行为，非实现细节
- [ ] 边界情况和错误场景已覆盖
